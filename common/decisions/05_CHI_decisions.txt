#Rework Author(s): TheGeneral

sco_category = {

	provide_development_assistance = {
		allowed = {
			OR = {
				original_tag = CHI
				original_tag = SOV
			}
		}
		icon = GFX_decision_sco_develp_button

		visible = {
			if = {
				limit = { original_tag = CHI }
				NOT = { has_completed_focus = CHI_Development_Assistance }
			}
			FROM = { is_sco = yes }
			is_sco = yes
		}

		available = {
			FROM = {
				NOT = {
					has_idea = SCO_development_assistance
				}
				NOT = {
					has_idea = SCO_development_assistance_2
				}
				has_war = no
			}
		}

		target_array = global.sco_members

		target_trigger = {
			country_exists = FROM
			ic_ratio = {
				tag = FROM
				ratio > 2
			}
			FROM = {
				NOT = {
					original_tag = CHI
					original_tag = SOV
				}
			}
		}

		custom_cost_trigger = {
			check_variable = { treasury > 1.4 }
		}

		custom_cost_text = cost_1_5

		days_remove = 90

		complete_effect = {
			set_temp_variable = { treasury_change = -1.5 }
			modify_treasury_effect = yes
			FROM = {
				add_timed_idea = {
					idea = SCO_development_assistance
					days = 90
				}
				country_event = { id = sco.11 }
			}
		}

		remove_effect = {
			set_temp_variable = { percent_change = 1.5 }
			set_temp_variable = { tag_index = ROOT.id }
			set_temp_variable = { influence_target = FROM.id }
			change_influence_percentage = yes
		}

		days_re_enable = 365

		ai_will_do = {
			base = 1
		}
	}


	provide_development_assistance_CHI = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_sco_develp_button

		visible = {
			is_sco = yes
			has_completed_focus = CHI_Development_Assistance
			FROM = { is_sco = yes }
		}

		available = {
			FROM = {
				NOT = {
					has_idea = SCO_development_assistance
				}
				NOT = {
					has_idea = SCO_development_assistance_2
				}
				has_war = no
			}
		}

		target_array = global.sco_members

		target_trigger = {
			country_exists = FROM
			FROM = {
				check_variable = { gdp_per_capita < ROOT.gdp_per_capita }
			}
			NOT = {
				original_tag = CHI
				original_tag = SOV
			}
		}

		custom_cost_trigger = {
			check_variable = { treasury > 1.4 }
		}

		custom_cost_text = cost_1_5

		complete_effect = {
			set_temp_variable = { treasury_change = -1.5 }
			modify_treasury_effect = yes
			FROM = {
				add_timed_idea = {
					idea = SCO_development_assistance_2
					days = 90
				}
				country_event = { id = sco.11 }
			}
		}

		days_remove = 90

		days_re_enable = 365

		remove_effect = {
			set_temp_variable = { percent_change = 2.25 }
			set_temp_variable = { tag_index = ROOT.id }
			set_temp_variable = { influence_target = FROM.id }
			change_influence_percentage = yes
		}
	}

	apply_to_join_as_an_observer = {
		icon = GFX_decision_sco_watch_button

		visible = {
			OR = {
				AND = { #Italy
					original_tag = ITA
					is_subject = no
					has_completed_focus = ITA_attempt_join_sco
					CHI = {
						has_country_flag = accepted_italy_as_sco_candidate
					}
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
				}
				AND = {
					original_tag = SWE
					SWE = { has_completed_focus = SWE_SCO_observer_status }
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
				}
				AND = { #Ukraine
					original_tag = UKR
					is_subject = no
					CHI = {
						has_completed_focus = CHI_Form_the_SCO
					}
					OR = {
						AND = {
							emerging_communist_state_are_in_power = yes
							has_completed_focus = UKR_commie_china_trade
						}
						AND = {
							emerging_reactionaries_are_in_power = yes
							has_completed_focus = UKR_party_regions_inaguration
						}
					}
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
				}
				AND = { #Libya
					original_tag = LBA
					is_subject = no
					CHI = {
						AND = {
							has_completed_focus = CHI_Form_the_SCO
							has_completed_focus = CHI_African_Railway_Projects
						}
					}
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
					LBA = {
						OR = {
							NOT = {
								OR = {
									has_completed_focus = LBA_strive_for_african_union
									has_completed_focus = LBA_approach_nato
								}
							}
							NOT = {
								salafist_caliphate_are_in_power = yes
							}
						}
					}
				}
				AND = { #Asia, Middle East and Africa
					is_subject = no
					CHI = {
						has_completed_focus = CHI_Form_the_SCO
					}
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
					if = { #ASEAN
						limit = { has_idea = ASEAN_Member }
						CHI = {
							has_completed_focus = CHI_Undermine_ASEAN
						}
					}
					if = { #Indian Subcontinent
						limit = {
							OR = {
								original_tag = RAJ
								original_tag = PAK
							}
						}
						NOT = {
							RAJ = {
								has_war_with = PAK
							}
							PAK = {
								has_war_with = RAJ
							}
						}
					}
					if = { #Korean Peninsula(North)
						limit = {
							original_tag = NKO
						}
						NOT = {
							has_war_with = KOR
						}
					}
					if = { #Korean Peninsula(South)
						limit = {
							original_tag = KOR
						}
						AND = {
							NOT = {
								has_war_with = NKO
							}
							OR = {
								KOR_has_progressive_government = yes
								KOR_has_liberal_government = yes
							}
						}
					}
					if = { #Middle East
						limit = {
							OR = {
								original_tag = ALA
								original_tag = AQY
								original_tag = BHR
								original_tag = DRU
								original_tag = FSA
								original_tag = HAM
								original_tag = HEZ
								original_tag = HOU
								original_tag = IRQ
								original_tag = ISI
								original_tag = JOR
								original_tag = KUR
								original_tag = PUK
								original_tag = KUW
								original_tag = LEB
								original_tag = NUS
								original_tag = OMA
								original_tag = PKK
								original_tag = QAT
								original_tag = ROJ
								original_tag = SAU
								original_tag = SYR
								original_tag = UAE
								original_tag = YEM
							}
						}
						OR = {
							PER = { is_sco = yes }
							TUR = { is_sco = yes }
							AND = {
								ISI = { exists = no }
								if = {
									limit = {
										OR = {
											original_tag = SYR
										}
									}
									has_completed_focus = SYR_russian_military_cooperation
								}
							}
						}
					}
					if = { #Nigeria
						limit = {
							original_tag = NIG
						}
						AND = {
							CHI = { has_completed_focus = CHI_African_Railway_Projects }
							OR = {
								AND = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = ROOT }
								}
								AND = {
									OR = {
										check_variable = { influence_array^0 = USA }
										check_variable = { influence_array^1 = USA }
									}
									check_variable = { influence_array^2 = ROOT }
								}
								AND = {
									OR = {
										check_variable = { influence_array^0 = USA }
										check_variable = { influence_array^1 = USA }
										check_variable = { influence_array^2 = USA }
									}
									check_variable = { influence_array^3 = ROOT }
								}
								AND = {
									OR = {
										check_variable = { influence_array^0 = USA }
										check_variable = { influence_array^1 = USA }
										check_variable = { influence_array^2 = USA }
										check_variable = { influence_array^3 = USA }
									}
									check_variable = { influence_array^4 = ROOT }
								}
								AND = {
									OR = {
										check_variable = { influence_array^0 = USA }
										check_variable = { influence_array^1 = USA }
										check_variable = { influence_array^2 = USA }
										check_variable = { influence_array^3 = USA }
										check_variable = { influence_array^4 = USA }
									}
									check_variable = { influence_array^5 = ROOT }
								}
								AND = {
									OR = {
										check_variable = { influence_array^0 = USA }
										check_variable = { influence_array^1 = USA }
										check_variable = { influence_array^2 = USA }
										check_variable = { influence_array^3 = USA }
										check_variable = { influence_array^4 = USA }
										check_variable = { influence_array^5 = USA }
									}
									check_variable = { influence_array^6 = ROOT }
								}
								is_influencer = no
							}
						}
					}
					if = { #Cameroon
						limit = {
							original_tag = CAM
						}
						OR = {
							AND = {
								check_variable = { influence_array^0 = USA }
								check_variable = { influence_array^1 = ROOT }
							}
							AND = {
								OR = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = USA }
								}
								check_variable = { influence_array^2 = ROOT }
							}
							AND = {
								OR = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = USA }
									check_variable = { influence_array^2 = USA }
								}
								check_variable = { influence_array^3 = ROOT }
							}
							AND = {
								OR = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = USA }
									check_variable = { influence_array^2 = USA }
									check_variable = { influence_array^3 = USA }
								}
								check_variable = { influence_array^4 = ROOT }
							}
							AND = {
								OR = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = USA }
									check_variable = { influence_array^2 = USA }
									check_variable = { influence_array^3 = USA }
									check_variable = { influence_array^4 = USA }
								}
								check_variable = { influence_array^5 = ROOT }
							}
							AND = {
								OR = {
									check_variable = { influence_array^0 = USA }
									check_variable = { influence_array^1 = USA }
									check_variable = { influence_array^2 = USA }
									check_variable = { influence_array^3 = USA }
									check_variable = { influence_array^4 = USA }
									check_variable = { influence_array^5 = USA }
								}
								check_variable = { influence_array^6 = ROOT }
							}
							is_influencer = no
						}
					}
					if = { #Other African Countries
						limit = {
							OR = {
								original_tag = DRC
								original_tag = EGY
								original_tag = ETH
								original_tag = SAF
								original_tag = SUD
							}
						}
						CHI = { has_completed_focus = CHI_African_Railway_Projects }
					}
				}
				AND = { #Americas
					is_subject = no
					OR = {
						original_tag = BRA
						original_tag = CUB
						original_tag = VEN
					}
					CHI = {
						has_completed_focus = CHI_Form_the_SCO
					}
					NOT = {
						has_idea = sco_member
						has_idea = sco_observer
						has_idea = sco_member_econ
						has_idea = sco_member_pol
						has_idea = sco_member_mil
						has_country_flag = SCO_observer_status_rejected
					}
					if = { #BRAZIL!!!!!
						limit = {
							original_tag = BRA
						}
						has_opinion = {
							target = CHI
							value > 74
						}
					}
					if = {
						limit = {
							original_tag = VEN
						}
						has_opinion = {
							target = CHI
							value > 99
						}
					}
					if = { #Cuba
						limit = {
							original_tag = CUB
						}
						has_opinion = {
							target = CHI
							value > 49
						}
					}
				}
			}
		}

		available = {
			if = {
				limit = { CHI = { has_government = communism } }
				NOT = { has_government = democratic }
			}
			NOT = {
				has_war_with = CHI
				has_idea = EU_member
				has_idea = NATO_member
				has_idea = Non_State_Actor
				has_idea = Lacks_International_Recognition
				has_idea = Massive_International_Sanctions
				has_idea = International_Sanctions
			}
			if = {
				limit = { CHI = { NOT = { has_completed_focus = CHI_Chinese_Dominance } } }
				NOT = { has_war_with = SOV }
			}
		}

		highlight_states = {
			highlight_states_trigger = {
				state = 540
			}
		}

		cost = 100

		complete_effect = {
			CHI = {
				country_event = { id = sco.1 }
			}
			if = {
				limit = {
					CHI = {
						NOT = { has_completed_focus = CHI_Chinese_Dominance }
					}
				}
				SOV = {
					country_event = { id = sco.1 }
				}
			}
		}

		days_remove = 90

		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = SCO_observer_status_rejected } }
				add_ideas = sco_observer
			}
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2
				has_government = communism
				CHI = { has_government = communism }
			}
			modifier = {
				add = 1
				check_variable = { chinese_aggression < -9 }
			}
			modifier = {
				add = 1
				check_variable = { chinese_aggression < -19 }
			}
			modifier = {
				add = 1
				check_variable = { chinese_aggression < -29 }
			}
			modifier = {
				factor = 0
				has_war = yes
			}
			modifier = {
				factor = 0
				SOV = { is_sco = yes }
				has_war_with = SOV
			}
			modifier = {
				factor = 0
				RAJ = { is_sco = yes }
				has_war_with = RAJ
			}
			modifier = {
				factor = 0
				PAK = { is_sco = yes }
				has_war_with = PAK
			}
			modifier = {
				factor = 0
				original_tag = NKO
				NKO = {
					OR = {
						has_idea = international_sanctions
						has_idea = Massive_International_Sanctions
					}
				}
			}
			modifier = {
				factor = 0
				original_tag = SOV
				CHI = { has_completed_focus = CHI_Chinese_Dominance }
			}
			modifier = {
				factor = 0
				has_opinion = {
					target = CHI
					value < -50
				}
			}
		}
	}

	give_up_observer_status = {
		icon = GFX_decision_sco_giveup_button

		visible = {
			has_idea = sco_observer
		}

		cost = 50

		complete_effect = {
			remove_ideas = sco_observer
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 1
				NOT = { has_government = communism }
				check_variable = { chinese_aggression > 19 }
			}
			modifier = {
				add = 10
				has_war_with = CHI
			}
			modifier = {
				add = 10
				SOV = { is_sco = yes }
				has_war_with = SOV
			}
			modifier = {
				add = 10
				RAJ = { is_sco = yes }
				has_war_with = RAJ
			}
			modifier = {
				add = 10
				PAK = { is_sco = yes }
				has_war_with = PAK
			}
		}
	}

	apply_to_join_as_a_member = {
		icon = GFX_decision_sco_member_button

		visible = {
			has_idea = sco_observer
			NOT = {
				has_idea = sco_member
				has_idea = sco_member_econ
				has_idea = sco_member_pol
				has_idea = sco_member_mil
			}
		}

		available = {
			is_subject = no
			if = {
				limit = { CHI = { has_government = communism } }
				NOT = { has_government = democratic }
			}
			NOT = {
				has_war_with = CHI
			}
			if = {
				limit = { CHI = { NOT = { has_completed_focus = CHI_Chinese_Dominance } } }
				NOT = { has_war_with = SOV }
			}
			if = {
				limit = { has_idea = ASEAN_Member }
				CHI = {
					has_completed_focus = CHI_Undermine_ASEAN
				}
			}
			if = {
				limit = { original_tag = UKR }
				OR = {
					AND = {
						emerging_communist_state_are_in_power = yes
						has_completed_focus = UKR_commie_china_trade
					}
					AND = {
						emerging_reactionaries_are_in_power = yes
						has_completed_focus = UKR_party_regions_inaguration
					}
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				state = 540
			}
		}

		cost = 200

		complete_effect = {
			CHI = {
				country_event = { id = sco.2 }
			}
			if = {
				limit = {
					CHI = {
						NOT = { has_completed_focus = CHI_Chinese_Dominance }
					}
				}
				SOV = {
					country_event = { id = sco.2 }
				}
			}
		}

		days_remove = 90

		remove_effect = {
			add_to_array = { global.sco_members = ROOT }
			SCO_dirty_update = yes
			if = {
				limit = {
					NOT = { has_country_flag = SCO_membership_rejected }
					CHI = { has_idea = sco_member_econ }
				}
				add_ideas = sco_member_econ
			}
			else_if = {
				limit = {
					NOT = { has_country_flag = SCO_membership_rejected }
					CHI = { has_idea = sco_member_pol }
				}
				add_ideas = sco_member_pol
			}
			else_if = {
				limit = {
					NOT = { has_country_flag = SCO_membership_rejected }
					CHI = { has_idea = sco_member_mil }
				}
				add_ideas = sco_member_mil
			}
			else_if = {
				limit = {
					NOT = { has_country_flag = SCO_membership_rejected }
					CHI = { has_idea = sco_member }
				}
				add_ideas = sco_member
			}
			if = {
				limit = {
					CHI = { has_completed_focus = CHI_Split_CSTO }
					is_in_faction_with = SOV
				}
				leave_faction = yes
			}
			if = {
				limit = { CHI = { has_completed_focus = CHI_An_Alliance_to_Rival_NATO } }
				CHI = { add_to_faction = ROOT }
			}
			if = {
				limit = { has_idea = ASEAN_Member }
				remove_ideas = ASEAN_Member
			}
			news_event = { id = sco.7 }
			remove_ideas = sco_observer
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 1
				CHI = { has_government = communism }
				has_government = communism
			}
			modifier = {
				add = 1
				check_variable = { chinese_aggression < -19 }
			}
			modifier = {
				add = 1
				check_variable = { chinese_aggression < -29 }
			}
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}

	give_up_membership_status = {
		allowed = { NOT = { original_tag = CHI } }
		icon = GFX_decision_sco_giveup_button

		visible = {
			is_sco = yes
		}

		available = {
			if = {
				limit = { original_tag = SOV }
				CHI = { has_completed_focus = CHI_Chinese_Dominance }
			}
		}

		cost = 50

		complete_effect = {
			remove_from_array = { global.sco_members = ROOT }
			if = {
				limit = { has_idea = sco_member }
				remove_ideas = sco_member
			}
			else_if = {
				limit = { has_idea = sco_member_econ }
				remove_ideas = sco_member_econ
			}
			else_if = {
				limit = { has_idea = sco_member_pol }
				remove_ideas = sco_member_pol
			}
			else_if = {
				limit = { has_idea = sco_member_mil }
				remove_ideas = sco_member_mil
			}
			if = {
				limit = { is_in_faction_with = CHI }
				leave_faction = yes
			}
			news_event = { id = sco.8 }
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 1
				NOT = { has_government = communism }
				check_variable = { chinese_aggression > 9 }
			}
			modifier = {
				add = 10
				has_war_with = CHI
			}
			modifier = {
				add = 10
				SOV = { is_sco = yes }
				has_war_with = SOV
			}
			modifier = {
				add = 10
				RAJ = { is_sco = yes }
				has_war_with = RAJ
			}
			modifier = {
				add = 10
				PAK = { is_sco = yes }
				has_war_with = PAK
			}
			modifier = {
				add = 10
				original_tag = SOV
				CHI = { has_completed_focus = CHI_Chinese_Dominance }
				check_variable = { chinese_aggression > 9 }
			}
			modifier = {
				add = 10
				has_opinion = {
					target = CHI
					value < -50
				}
			}
		}
	}

	host_joint_exercises = {
		allowed = {
			OR = {
				original_tag = CHI
				original_tag = SOV
			}
		}

		icon = GFX_decision_sco_exersises_button

		visible = {
			is_sco = yes
			OR = {
				original_tag = CHI
				AND = {
					original_tag = SOV
					CHI = {
						NOT = { has_completed_focus = CHI_Chinese_Dominance }
					}
				}
			}
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = SCO_peace_tt
				NOT = {
					any_of_scopes = {
						array = global.sco_members
						is_sco = yes
						has_war = yes
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = SCO_any_drills_tt
				NOT = {
					any_of_scopes = {
						array = global.sco_members
						OR = {
							has_country_flag = SCO_joint_exercises_accepted
							has_country_flag = SCO_joint_exercises_offered
							has_idea = SCO_joint_drill
							has_idea = SCO_joint_drill_1
							has_idea = SCO_joint_drill_specific
						}
					}
				}
			}
		}

		custom_cost_trigger = {
			command_power > 40
		}

		custom_cost_text = command_power_more_than_40

		complete_effect = {
			add_command_power = -40
			if = {
				limit = { has_completed_focus = CHI_Strengthen_Security_Framework }
				every_country = {
					limit = { is_sco = yes }
					add_timed_idea = {
						idea = SCO_joint_drill_1
						days = 21
					}
				}
			}
			else = {
				every_country = {
					limit = { is_sco = yes }
					add_timed_idea = {
						idea = SCO_joint_drill
						days = 21
					}
				}
			}
			news_event = { days = 1 id = sco.6 }
		}

		days_remove = 14

		days_re_enable = 730

		fixed_random_seed = no

		remove_effect = {
			random_army_leader = {
				add_skill_level = 1
			}
		}

		ai_will_do = {
			base = 1
		}
	}

	offer_joint_exercises_specific = {

		icon = GFX_decision_sco_exersises_button

		visible = {
			is_sco = yes
			FROM = { is_sco = yes }
		}

		target_array = global.sco_members

		target_trigger = {
			country_exists = FROM
		}

		available = {
			has_war = no
			FROM = { has_war = no }
			custom_trigger_tooltip = {
				tooltip = SCO_drills_tt
				NOT = { has_country_flag = SCO_joint_exercises_accepted }
				NOT = { has_country_flag = SCO_joint_exercises_offered }
				NOT = { has_idea = SCO_joint_drill }
				NOT = { has_idea = SCO_joint_drill_1 }
				NOT = { has_idea = SCO_joint_drill_specific }
			}
			FROM = {
				custom_trigger_tooltip = {
					tooltip = SCO_drills_tt
					NOT = { has_country_flag = SCO_joint_exercises_accepted }
					NOT = { has_country_flag = SCO_joint_exercises_offered }
					NOT = { has_idea = SCO_joint_drill }
					NOT = { has_idea = SCO_joint_drill_1 }
					NOT = { has_idea = SCO_joint_drill_specific }
				}
			}
			if = {
				limit = {
					original_tag = CHI
					FROM = { original_tag = PAK }
				}
				NOT = { has_completed_focus = CHI_End_Joint_Military_Exercises }
			}
		}

		cost = 50

		days_re_enable = 365

		complete_effect = {
			set_country_flag = SCO_joint_exercises_offered
			FROM = {
				country_event = {
					id = sco.3
				}
			}
		}

		ai_will_do = {
			base = 1
		}
	}

	start_joint_exercises_specific = {

		icon = GFX_decision_sco_exersises_button

		visible = {
			is_sco = yes
			has_country_flag = SCO_joint_exercises_offered
		}

		target_array = global.sco_members

		target_trigger = {
			country_exists = FROM
			FROM = { has_country_flag = SCO_joint_exercises_accepted }
		}

		available = {
			has_war = no
			FROM = { has_war = no }
		}

		custom_cost_trigger = {
			command_power > 20
		}

		custom_cost_text = command_power_more_than_20

		complete_effect = {
			add_command_power = -20
			ROOT = {
				add_timed_idea = {
					idea = SCO_joint_drill_specific
					days = 14
				}
			}
			FROM = {
				add_timed_idea = {
					idea = SCO_joint_drill_specific
					days = 14
				}
			}
		}

		days_remove = 14

		remove_effect = {
			add_opinion_modifier = {
				target = FROM
				modifier = military_cooperation
			}
			clr_country_flag = SCO_joint_exercises_offered
			FROM = {
				add_opinion_modifier = {
					target = ROOT
					modifier = military_cooperation
				}
				clr_country_flag = SCO_joint_exercises_accepted
			}
		}

		ai_will_do = {
			base = 1
		}

	}
}


arunachal_war_decisions_category = {
	prepare_army_training_arunachal = {
		available = {
			has_war = no
			has_equipment = { infantry_weapons_type > 30 }
			has_equipment = { cnc_equipment_type > 7 }
		}

		days_remove = 15
		days_re_enable = 20
		custom_cost_text = command_power_more_than_20
		fixed_random_seed = no
		custom_cost_trigger = {
			command_power > 20
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_army_training_arunachal"
			add_command_power = -20
			custom_effect_tooltip = arunachal_increase_readiness_TT

			add_equipment_to_stockpile = {
				type = infantry_weapons_type
				amount = -30
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment_type
				amount = -7
			}

			hidden_effect = {
				random_list = {
					10 = { add_to_variable = { THIS.arunachal_readiness = 5 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 10 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 20 } }
				}

				clamp_variable = {
					var = THIS.arunachal_readiness
					min = 0
					max = 100
				}

				# 10% chance for bad event
				random = {
					chance = 10
					country_event = { id = arunachal_war.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				threat < 0.5
			}
			modifier = {
				factor = 0
				check_variable = { THIS.arunachal_readiness > 80 }
			}
		}
	}

	prepare_mech_army_training_arunachal = {
		available = {
			has_war = no
			has_equipment = { medium_tank_amphibious_chassis > 5 }
			has_equipment = { medium_tank_chassis > 3 }
		}

		days_remove = 15
		days_re_enable = 20
		custom_cost_text = command_power_more_than_20
		fixed_random_seed = no
		custom_cost_trigger = {
			command_power > 20
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_mech_army_training_arunachal"
			add_command_power = -20
			custom_effect_tooltip = arunachal_increase_readiness_TT

			add_equipment_to_stockpile = {
				type = medium_tank_amphibious_chassis
				amount = -5
			}
			add_equipment_to_stockpile = {
				type = medium_tank_chassis
				amount = -3
			}

			hidden_effect = {
				random_list = {
					10 = { add_to_variable = { THIS.arunachal_readiness = 10 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 20 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 30 } }
				}

				clamp_variable = {
					var = THIS.arunachal_readiness
					min = 0
					max = 100
				}

				# 10% chance for bad event
				random = {
					chance = 10
					country_event = { id = arunachal_war.2 days = 1 }
				}
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				threat < 0.5
			}
			modifier = {
				factor = 0
				check_variable = { THIS.arunachal_readiness > 80 }
			}
		}
	}

	prepare_arty_army_training_arunachal = {
		available = {
			has_war = no
			has_equipment = { artillery_equipment > 5 }
		}

		days_remove = 15
		days_re_enable = 20
		custom_cost_text = command_power_more_than_20
		fixed_random_seed = no
		custom_cost_trigger = {
			command_power > 20
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_arty_army_training_arunachal"
			add_command_power = -20
			custom_effect_tooltip = arunachal_readiness_TT

			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = -5
			}

			hidden_effect = {
				random_list = {
					10 = { add_to_variable = { THIS.arunachal_readiness = 5 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 10 } }
					10 = { add_to_variable = { THIS.arunachal_readiness = 15 } }
				}
			}

			clamp_variable = {
				var = THIS.arunachal_readiness
				min = 0
				max = 100
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				threat < 0.5
			}
			modifier = {
				factor = 0
				check_variable = { THIS.arunachal_readiness > 80 }
			}
		}
	}

	prepare_attack_arunachal = {
		available = {
			has_war = no
			NOT = {
				has_decision = prepare_shelling_china
			}
		}

		days_remove = 15
		days_re_enable = 20
		custom_cost_text = command_power_more_than_20
		fixed_random_seed = no
		custom_cost_trigger = {
			command_power > 20
		}

		remove_effect = {
			add_command_power = -20

			custom_effect_tooltip = arunachal_increase_tension_TT

			hidden_effect = {
				log = "[GetDateText]: [Root.GetName]: Decision prepare_attack_arunachal"
				random_list = {
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 10 } }
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 20 } }
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 25 } }
				}

				clamp_variable = {
					var = kasmir_tension_china
					min = 0
					max = 100
				}

				# 10% chance for bad event
				random = {
					chance = 10
					country_event = { id = kasmir_war.3 days = 1 }
				}
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				threat < 0.5
			}
			modifier = {
				factor = 0
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
		}
	}

	prepare_shelling_arunachal = {
		available = {
			has_war = no
			NOT = {
				has_decision = prepare_attack_arunachal
			}
		}

		days_remove = 15
		days_re_enable = 20
		custom_cost_text = command_power_more_than_20
		fixed_random_seed = no
		custom_cost_trigger = {
			command_power > 20
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_shelling_arunachal"
			add_command_power = -20

			custom_effect_tooltip = arunachal_increase_tension_TT

			hidden_effect = {
				random_list = {
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 10 } }
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 20 } }
					10 = { add_to_variable = { GLOBAL.arunachal_tension = 25 } }
				}

				clamp_variable = {
					var = GLOBAL.arunachal_tension
					min = 0
					max = 100
				}

				# 10% chance for bad event
				random = {
					chance = 10
					country_event = { id = arunachal_war.3 days = 1 }
				}
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				threat < 0.5
			}
			modifier = {
				factor = 0
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
		}
	}

	attack_475_from_589 = {
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 475
			}
		}

		visible = {
			controls_state = 589
			NOT = { controls_state = 475 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_475_from_589"
			set_country_flag = Attacking_in_Arunachal
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = not_attacking_on_arunachal_TT
				NOT = { has_country_flag = Attacking_in_Arunachal }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_tension_TT
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_readiness_TT
				check_variable = { arunachal_readiness > 60 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_475_from_589"
			if = {
				limit = {
					has_border_war_between = {
						attacker = 589
						defender = 475
					}
				}

				country_event = arunachal_war.4
			}
			else = {
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 589
						num_provinces = 2
						on_win = arunachal_war.20
						on_lose = arunachal_war.21
						on_cancel = arunachal_war.22
					}
					defender = {
						state = 475
						num_provinces = 3
						on_win = arunachal_war.24
						on_lose = arunachal_war.23
						on_cancel = arunachal_war.25
					}
				}
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				threat < 0.5
			}
		}
	}

	attack_589_from_475 = {
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 589
			}
		}

		visible = {
			controls_state = 475
			NOT = { controls_state = 589 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_589_from_475"
			set_country_flag = Attacking_in_Arunachal
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = not_attacking_on_arunachal_TT
				NOT = { has_country_flag = Attacking_in_Arunachal }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_tension_TT
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_readiness_TT
				check_variable = { arunachal_readiness > 60 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_589_from_475"
			if = {
				limit = {
					has_border_war_between = {
						attacker = 475
						defender = 589
					}
				}

				country_event = arunachal_war.4
			}
			else = {
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 475
						num_provinces = 3
						on_win = arunachal_war.20
						on_lose = arunachal_war.21
						on_cancel = arunachal_war.22
					}
					defender = {
						state = 589
						num_provinces = 2
						on_win = arunachal_war.24
						on_lose = arunachal_war.23
						on_cancel = arunachal_war.25
					}
				}
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				threat < 0.5
			}
		}
	}

	attack_982_from_589 = {
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 982
			}
		}

		visible = {
			controls_state = 589
			NOT = { controls_state = 982 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_475_from_589"
			set_country_flag = Attacking_in_Arunachal
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = not_attacking_on_arunachal_TT
				NOT = { has_country_flag = Attacking_in_Arunachal }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_tension_TT
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_readiness_TT
				check_variable = { arunachal_readiness > 60 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_475_from_589"
			if = {
				limit = {
					has_border_war_between = {
						attacker = 589
						defender = 982
					}
				}

				country_event = arunachal_war.4
			}
			else = {
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 589
						num_provinces = 2
						on_win = arunachal_war.20
						on_lose = arunachal_war.21
						on_cancel = arunachal_war.22
					}
					defender = {
						state = 982
						num_provinces = 1
						on_win = arunachal_war.24
						on_lose = arunachal_war.23
						on_cancel = arunachal_war.25
					}
				}
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				threat < 0.5
			}
		}
	}

	attack_589_from_982 = {
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 589
			}
		}

		visible = {
			controls_state = 982
			NOT = { controls_state = 589 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_589_from_982"
			set_country_flag = Attacking_in_Arunachal
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = not_attacking_on_arunachal_TT
				NOT = { has_country_flag = Attacking_in_Arunachal }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_tension_TT
				check_variable = { GLOBAL.arunachal_tension > 80 }
			}
			custom_trigger_tooltip = {
				tooltip = arunachal_readiness_TT
				check_variable = { arunachal_readiness > 60 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_589_from_982"
			if = {
				limit = {
					has_border_war_between = {
						attacker = 982
						defender = 589
					}
				}

				country_event = arunachal_war.4
			}
			else = {
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 982
						num_provinces = 1
						on_win = arunachal_war.20
						on_lose = arunachal_war.21
						on_cancel = arunachal_war.22
					}
					defender = {
						state = 589
						num_provinces = 2
						on_win = arunachal_war.24
						on_lose = arunachal_war.23
						on_cancel = arunachal_war.25
					}
				}
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				threat < 0.5
			}
		}
	}

	grab_475 = {
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 475
			}
		}

		visible = {
			NOT = { controls_state = 475 }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = more_than_80_arunachal_progress_TT
				check_variable = { THIS.Arunachal_war_progress > 79 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_475"
			subtract_from_variable = { THIS.Arunachal_war_progress = 80 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_475"
			transfer_state = 475
			if = {
				limit = { tag = RAJ }
				CHI = { country_event = arunachal_war.30 }
			}
			else = {
				RAJ = { country_event = arunachal_war.30 }
			}
		}

		ai_will_do = { factor = 10 }
	}

	grab_589 = {
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 589
			}
		}

		visible = {
			NOT = { controls_state = 589 }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = more_than_80_arunachal_progress_TT
				check_variable = { THIS.Arunachal_war_progress > 79 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_589"
			subtract_from_variable = { THIS.Arunachal_war_progress = 80 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_589"
			transfer_state = 589
			if = {
				limit = { tag = RAJ }
				CHI = { country_event = arunachal_war.30 }
			}
			else = {
				RAJ = { country_event = arunachal_war.30 }
			}
		}

		ai_will_do = { factor = 10 }
	}

	grab_982 = {
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = {
			highlight_states_trigger = {
				state = 982
			}
		}

		visible = {
			NOT = { controls_state = 982 }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = more_than_80_arunachal_progress_TT
				check_variable = { THIS.Arunachal_war_progress > 79 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_982"
			subtract_from_variable = { THIS.Arunachal_war_progress = 80 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_982"
			transfer_state = 982
			if = {
				limit = { tag = RAJ }
				CHI = { country_event = arunachal_war.30 }
			}
			else = {
				RAJ = { country_event = arunachal_war.30 }
			}
		}

		ai_will_do = { factor = 10 }
	}

	extort_xp_arunachal = {
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		available = {
			custom_trigger_tooltip = {
				tooltip = more_than_50_arunachal_progress_TT
				check_variable = { THIS.Arunachal_war_progress > 49 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision extort_xp_arunachal"
			subtract_from_variable = { THIS.Arunachal_war_progress = 50 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove decision extort_xp_arunachal"
			army_experience = 30
		}

		ai_will_do = { factor = 5 }
	}
}

CHI_national_commitee_decisions_category = {

	CHI_current_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fixed_random_seed = no
		days_mission_timeout = 6

		available = {
			hidden_trigger = {
				always = no
			}
		}

		activation = {
			has_country_flag = CHI_national_congress_started
		}

		timeout_effect = {
			clr_country_flag = CHI_national_congress_started
			if = {
				limit = { has_country_flag = CHI_16_nc_needs_to_be_started }
				clr_country_flag = CHI_16_nc_needs_to_be_started
				set_country_flag = CHI_17_nc_needs_to_be_started
				activate_mission = CHI_17_national_congress_mission
				if = {
					limit = { NOT = { has_country_flag = CHI_hu_jintao_became_leader } }
					set_country_flag = CHI_hu_jintao_became_leader
					set_variable = { CHI_ic_leader = 2 }
					kill_country_leader = yes
					create_country_leader = {
						name = "Hu Jintao"
						picture = "Hu_jintao.dds"
						ideology = Communist-State
						traits = {
							emerging_Communist-State
						}
					}
				}
			}

			else_if = {
				limit = { has_country_flag = CHI_17_nc_needs_to_be_started }
				clr_country_flag = CHI_17_nc_needs_to_be_started
				set_country_flag = CHI_18_nc_needs_to_be_started
				activate_mission = CHI_18_national_congress_mission
			}

			else_if = {
				limit = { has_country_flag = CHI_18_nc_needs_to_be_started }
				clr_country_flag = CHI_18_nc_needs_to_be_started
				set_country_flag = CHI_19_nc_needs_to_be_started
				activate_mission = CHI_19_national_congress_mission
				if = {
					limit = { NOT = { has_country_flag = CHI_xi_jinping_became_leader } }
					set_country_flag = CHI_xi_jinping_became_leader
					set_variable = { CHI_ic_leader = 3 }
					kill_country_leader = yes
					create_country_leader = {
						name = "Xi Jinping"
						picture = "Xi_Jinping.dds"
						ideology = Communist-State
						traits = {
							ex_engineer
							emerging_Communist-State
							rational
							polished
							honest
							capable
						}
					}
				}
			}

			else_if = {
				limit = { has_country_flag = CHI_19_nc_needs_to_be_started }
				clr_country_flag = CHI_19_nc_needs_to_be_started
				set_country_flag = CHI_20_nc_needs_to_be_started
				activate_mission = CHI_20_national_congress_mission
			}

			else_if = {
				limit = { has_country_flag = CHI_20_nc_needs_to_be_started }
				clr_country_flag = CHI_20_nc_needs_to_be_started
				set_country_flag = CHI_21_nc_needs_to_be_started
				activate_mission = CHI_21_national_congress_mission
			}

			CHI_assign_all_secretaries = yes
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_16_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		is_good = yes

		days_mission_timeout = 1042

		available = {
			hidden_trigger = {
				always = no
			}
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_16_nc_needs_to_be_started
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_17_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		days_mission_timeout = 1796

		activation = {
			has_country_flag = CHI_17_nc_needs_to_be_started
		}

		available = {
			hidden_trigger = {
				always = no
			}
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_17_nc_needs_to_be_started
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_18_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		days_mission_timeout = 1845

		available = {
			hidden_trigger = {
				always = no
			}
		}

		activation = {
			has_country_flag = CHI_18_nc_needs_to_be_started
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_18_nc_needs_to_be_started
			# NSC established at 18th Congress (2012)
			set_country_flag = CHI_ic_nsc_unlocked
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_19_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		days_mission_timeout = 1799

		available = {
			hidden_trigger = {
				always = no
			}
		}

		activation = {
			has_country_flag = CHI_19_nc_needs_to_be_started
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_19_nc_needs_to_be_started
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_20_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		days_mission_timeout = 1818

		available = {
			hidden_trigger = {
				always = no
			}
		}

		activation = {
			has_country_flag = CHI_20_nc_needs_to_be_started
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_20_nc_needs_to_be_started
			# Society Work Department established at 20th Congress (2022)
			set_country_flag = CHI_ic_social_unlocked
		}

		ai_will_do = {
			base = 1
		}
	}
	CHI_21_national_congress_mission = {
		allowed = { original_tag = CHI }
		icon = x

		fire_only_once = yes

		days_mission_timeout = 1824

		available = {
			hidden_trigger = {
				always = no
			}
		}

		activation = {
			has_country_flag = CHI_21_nc_needs_to_be_started
		}

		timeout_effect = {
			CHI_reset_ic_commissions = yes
			country_event = { id = china_national_committee.02 }
			activate_mission = CHI_current_congress_mission
			set_country_flag = CHI_national_congress_started
			set_country_flag = CHI_21_nc_needs_to_be_started
		}

		ai_will_do = {
			base = 1
		}
	}


	CHI_jiang_policy_three_represents = {
		allowed = { original_tag = CHI }
		icon = x

		days_remove = 365
		visible = {
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_jiang_policy_three_represents complete"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_jiang_policy_three_represents remove"
		}

		modifier = {
			production_speed_buildings_factor = 0.125
			trade_opinion_factor = 0.15
			research_speed_factor = 0.075
			civilian_industry_tax_modifier = 0.05
			bureaucracy_cost_multiplier_modifier = 0.15
		}

		ai_will_do = {
			base = 1
		}
	}
}

CHI_SAR_decisions_category = {
	CHI_demand_HKG_press_controls = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_speech

		cost = 50
		days_remove = 180

		visible = {
			HKG = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = HKG }
			HKG = { check_variable = { CHI_HKG_autonomy_index > 10 } }
		}

		cancel_trigger = {
			HKG = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_HKG_press_controls"
			HKG = {
				add_to_variable = { CHI_HKG_autonomy_index = -10 }
				update_HKG_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			random_list = {
				40 = { HKG = { country_event = { id = CHI_SAR_relations.20 days = 7 } } }
				60 = {}
			}
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
			modifier = {
				factor = 2
				HKG = { check_variable = { CHI_HKG_autonomy_index > 50 } }
			}
		}
	}

	CHI_demand_HKG_extradition = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_spy_operation

		cost = 75
		days_remove = 365

		visible = {
			HKG = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = HKG }
			HKG = { check_variable = { CHI_HKG_autonomy_index > 20 } }
		}

		cancel_trigger = {
			HKG = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_HKG_extradition"
			HKG = {
				add_to_variable = { CHI_HKG_autonomy_index = -15 }
				update_HKG_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			HKG = { country_event = { id = CHI_SAR_relations.20 days = 14 } }
		}

		ai_will_do = {
			base = 3
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_HKG_electoral_vetting = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_political_discourse

		cost = 100
		days_remove = 365

		visible = {
			HKG = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = HKG }
			HKG = { check_variable = { CHI_HKG_autonomy_index > 30 } }
		}

		cancel_trigger = {
			HKG = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_HKG_electoral_vetting"
			HKG = {
				add_to_variable = { CHI_HKG_autonomy_index = -20 }
				update_HKG_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 10 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			HKG = { country_event = { id = CHI_SAR_relations.20 days = 7 } }
			random_list = {
				50 = { country_event = { id = CHI_SAR_relations.23 days = 30 } }
				50 = {}
			}
		}

		ai_will_do = {
			base = 2
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_HKG_national_security_law = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_secret_rearmament

		cost = 150
		days_remove = 730

		visible = {
			HKG = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = HKG }
			HKG = { check_variable = { CHI_HKG_autonomy_index > 20 } }
			NOT = { HKG = { has_idea = HKG_national_security_law_idea } }
		}

		cancel_trigger = {
			HKG = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_HKG_national_security_law"
			HKG = {
				add_to_variable = { CHI_HKG_autonomy_index = -30 }
				update_HKG_autonomy_modifiers = yes
				add_ideas = HKG_national_security_law_idea
			}
			set_temp_variable = { percent_change = 20 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			HKG = { country_event = { id = CHI_SAR_relations.20 days = 3 } }
			country_event = { id = CHI_SAR_relations.23 days = 14 }
		}

		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
			modifier = {
				factor = 2
				HKG = { influence_higher_60 = yes }
			}
		}
	}

	CHI_invest_HKG_greater_bay_area = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_industry

		cost = 100
		days_remove = 365

		visible = {
			HKG = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = HKG }
			NOT = { HKG = { has_idea = HKG_CHI_economic_support } }
		}

		cancel_trigger = {
			HKG = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_invest_HKG_greater_bay_area"
			HKG = {
				add_to_variable = { CHI_HKG_autonomy_index = 15 }
				update_HKG_autonomy_modifiers = yes
				country_event = { id = CHI_SAR_relations.27 days = 30 }
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			add_timed_idea = { idea = CHI_HKG_investment_returns days = 730 }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 2
				check_variable = { treasury > 100 }
			}
		}
	}

	###############################################
	### CHI DEMANDS — MAC ###
	###############################################

	CHI_demand_MAC_regulatory_alignment = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_political_discourse

		cost = 50
		days_remove = 180

		visible = {
			MAC = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = MAC }
			MAC = { check_variable = { CHI_MAC_autonomy_index > 10 } }
		}

		cancel_trigger = {
			MAC = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_MAC_regulatory_alignment"
			MAC = {
				add_to_variable = { CHI_MAC_autonomy_index = -10 }
				update_MAC_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = MAC }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_MAC_cultural_sinicization = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_speech

		cost = 75
		days_remove = 365

		visible = {
			MAC = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = MAC }
			MAC = { check_variable = { CHI_MAC_autonomy_index > 15 } }
		}

		cancel_trigger = {
			MAC = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_MAC_cultural_sinicization"
			MAC = {
				add_to_variable = { CHI_MAC_autonomy_index = -15 }
				update_MAC_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = MAC }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 3
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_MAC_security_cooperation = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_secret_rearmament

		cost = 100
		days_remove = 365

		visible = {
			MAC = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = MAC }
			MAC = { check_variable = { CHI_MAC_autonomy_index > 20 } }
		}

		cancel_trigger = {
			MAC = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_MAC_security_cooperation"
			MAC = {
				add_to_variable = { CHI_MAC_autonomy_index = -20 }
				update_MAC_autonomy_modifiers = yes
			}
			set_temp_variable = { percent_change = 12 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = MAC }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 2
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_invest_MAC_integrated_resort_zone = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_industry

		cost = 100
		days_remove = 365

		visible = {
			MAC = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = MAC }
			NOT = { MAC = { has_idea = MAC_diversification_boom } }
		}

		cancel_trigger = {
			MAC = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_invest_MAC_integrated_resort_zone"
			MAC = {
				add_to_variable = { CHI_MAC_autonomy_index = 15 }
				update_MAC_autonomy_modifiers = yes
				add_timed_idea = { idea = MAC_diversification_boom days = 730 }
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = MAC }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 4
			modifier = {
				factor = 2
				check_variable = { treasury > 100 }
			}
		}
	}

	###############################################
	### CHI DEMANDS — TIB ###
	###############################################

	CHI_demand_TIB_religious_restrictions = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_speech

		cost = 75
		days_remove = 365

		visible = {
			TIB = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = TIB }
			TIB = { check_variable = { CHI_TIB_autonomy_index > 10 } }
		}

		cancel_trigger = {
			TIB = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_TIB_religious_restrictions"
			TIB = {
				add_to_variable = { CHI_TIB_autonomy_index = -15 }
				add_to_variable = { sinicization_level = 10 }
				update_TIB_sinicization_modifiers = yes
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TIB }
			change_influence_percentage = yes
			TIB = { country_event = { id = CHI_SAR_relations.26 days = 30 } }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_TIB_settlement_expansion = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_construction

		cost = 75
		days_remove = 365

		visible = {
			TIB = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = TIB }
			TIB = { check_variable = { CHI_TIB_autonomy_index > 5 } }
		}

		cancel_trigger = {
			TIB = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_TIB_settlement_expansion"
			TIB = {
				add_to_variable = { CHI_TIB_autonomy_index = -10 }
				add_to_variable = { sinicization_level = 15 }
				update_TIB_sinicization_modifiers = yes
			}
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TIB }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_TIB_security_crackdown = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_secret_rearmament

		cost = 100
		days_remove = 365

		visible = {
			TIB = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = TIB }
			TIB = { check_variable = { CHI_TIB_autonomy_index > 15 } }
		}

		cancel_trigger = {
			TIB = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_TIB_security_crackdown"
			TIB = {
				add_to_variable = { CHI_TIB_autonomy_index = -20 }
				add_to_variable = { sinicization_level = 15 }
				update_TIB_sinicization_modifiers = yes
				country_event = { id = CHI_SAR_relations.26 days = 7 }
			}
			set_temp_variable = { percent_change = 10 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TIB }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 3
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_invest_TIB_infrastructure = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_industry

		cost = 100
		days_remove = 365

		visible = {
			TIB = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = TIB }
			NOT = { TIB = { has_idea = TIB_infrastructure_investment } }
		}

		cancel_trigger = {
			TIB = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_invest_TIB_infrastructure"
			TIB = {
				add_to_variable = { CHI_TIB_autonomy_index = 10 }
				add_to_variable = { sinicization_level = 5 }
				update_TIB_sinicization_modifiers = yes
				add_timed_idea = { idea = TIB_infrastructure_investment days = 730 }
			}
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TIB }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 2
				check_variable = { treasury > 100 }
			}
		}
	}

	###############################################
	### CHI DEMANDS — ETK ###
	###############################################

	CHI_demand_ETK_reeducation = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_speech

		cost = 75
		days_remove = 365

		visible = {
			ETK = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = ETK }
			ETK = { check_variable = { CHI_ETK_autonomy_index > 10 } }
		}

		cancel_trigger = {
			ETK = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_ETK_reeducation"
			ETK = {
				add_to_variable = { CHI_ETK_autonomy_index = -25 }
				add_to_variable = { sinicization_level = 20 }
				update_ETK_sinicization_modifiers = yes
				add_ideas = ETK_surveillance_state
			}
			set_temp_variable = { percent_change = 12 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = ETK }
			change_influence_percentage = yes
			country_event = { id = CHI_SAR_relations.24 days = 180 }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_ETK_surveillance_expansion = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_secret_rearmament

		cost = 50
		days_remove = 180

		visible = {
			ETK = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = ETK }
			ETK = { check_variable = { CHI_ETK_autonomy_index > 5 } }
		}

		cancel_trigger = {
			ETK = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_ETK_surveillance_expansion"
			ETK = {
				add_to_variable = { CHI_ETK_autonomy_index = -15 }
				add_to_variable = { sinicization_level = 15 }
				update_ETK_sinicization_modifiers = yes
			}
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = ETK }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_demand_ETK_labor_transfer = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_construction

		cost = 75
		days_remove = 365

		visible = {
			ETK = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = ETK }
			ETK = { check_variable = { CHI_ETK_autonomy_index > 10 } }
		}

		cancel_trigger = {
			ETK = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_demand_ETK_labor_transfer"
			ETK = {
				add_to_variable = { CHI_ETK_autonomy_index = -20 }
				add_to_variable = { sinicization_level = 15 }
				update_ETK_sinicization_modifiers = yes
			}
			set_temp_variable = { percent_change = 10 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = ETK }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 4
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_invest_ETK_development_zones = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_industry

		cost = 100
		days_remove = 365

		visible = {
			ETK = { is_subject_of = CHI  exists = yes }
		}

		available = {
			NOT = { has_war_with = ETK }
			NOT = { ETK = { has_idea = ETK_development_zones } }
		}

		cancel_trigger = {
			ETK = { NOT = { is_subject_of = CHI } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_invest_ETK_development_zones"
			ETK = {
				add_to_variable = { CHI_ETK_autonomy_index = 10 }
				add_to_variable = { sinicization_level = 10 }
				update_ETK_sinicization_modifiers = yes
				add_timed_idea = { idea = ETK_development_zones days = 730 }
			}
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = ETK }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 4
			modifier = {
				factor = 2
				check_variable = { treasury > 100 }
			}
		}
	}

	###############################################
	### HKG REQUESTS ###
	###############################################

	HKG_request_economic_support = {
		allowed = { original_tag = HKG }
		icon = GFX_decision_generic_industry

		cost = 50
		days_remove = 180

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = HKG_economic_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HKG_request_economic_support"
			set_country_flag = { flag = HKG_economic_request_pending days = 365 }
			CHI = { country_event = { id = CHI_SAR_relations.1 days = 3 } }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 2
				check_variable = { CHI_HKG_autonomy_index < 50 }
			}
		}
	}

	HKG_request_autonomy_guarantee = {
		allowed = { original_tag = HKG }
		icon = GFX_decision_international_treaty

		cost = 75
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = HKG_autonomy_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HKG_request_autonomy_guarantee"
			set_country_flag = { flag = HKG_autonomy_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.2 days = 3 } }
		}

		ai_will_do = {
			base = 3
			modifier = {
				factor = 3
				check_variable = { CHI_HKG_autonomy_index < 40 }
			}
		}
	}

	HKG_request_universal_suffrage = {
		allowed = { original_tag = HKG }
		icon = GFX_decision_democracy_acceptance

		cost = 100
		days_remove = 730

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = HKG_suffrage_request_pending }
			has_idea = Lacks_Universal_Suffrage
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HKG_request_universal_suffrage"
			set_country_flag = { flag = HKG_suffrage_request_pending days = 1460 }
			CHI = { country_event = { id = CHI_SAR_relations.3 days = 3 } }
		}

		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	HKG_organize_protest = {
		allowed = { original_tag = HKG }
		icon = GFX_decision_generic_protests

		days_remove = 180

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			check_variable = { CHI_HKG_autonomy_index < 70 }
			NOT = { has_idea = HKG_protest_movement }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HKG_organize_protest"
			add_to_variable = { CHI_HKG_autonomy_index = 20 }
			update_HKG_autonomy_modifiers = yes
			add_stability = -0.05
			add_timed_idea = { idea = HKG_protest_movement days = 180 }
			set_temp_variable = { percent_change = -10 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = HKG }
			change_influence_percentage = yes
			CHI = { country_event = { id = CHI_SAR_relations.21 days = 1 } }
		}

		ai_will_do = {
			base = 2
			modifier = {
				factor = 3
				check_variable = { CHI_HKG_autonomy_index < 30 }
			}
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	###############################################
	### MAC REQUESTS ###
	###############################################

	MAC_request_economic_diversification = {
		allowed = { original_tag = MAC }
		icon = GFX_decision_generic_industry

		cost = 50
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = MAC_economic_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision MAC_request_economic_diversification"
			set_country_flag = { flag = MAC_economic_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.6 days = 3 } }
		}

		ai_will_do = {
			base = 5
		}
	}

	MAC_request_cultural_preservation = {
		allowed = { original_tag = MAC }
		icon = GFX_decision_generic_political_discourse

		cost = 50
		days_remove = 730

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = MAC_cultural_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision MAC_request_cultural_preservation"
			set_country_flag = { flag = MAC_cultural_request_pending days = 1095 }
			CHI = { country_event = { id = CHI_SAR_relations.7 days = 3 } }
		}

		ai_will_do = {
			base = 3
		}
	}

	MAC_request_integrated_resort_expansion = {
		allowed = { original_tag = MAC }
		icon = GFX_decision_generic_economy

		cost = 75
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = MAC_resort_request_pending }
			NOT = { has_idea = MAC_diversification_boom }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision MAC_request_integrated_resort_expansion"
			set_country_flag = { flag = MAC_resort_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.8 days = 3 } }
		}

		ai_will_do = {
			base = 4
		}
	}

	###############################################
	### TIB REQUESTS ###
	###############################################

	TIB_request_religious_freedom = {
		allowed = { original_tag = TIB }
		icon = GFX_decision_generic_political_discourse

		cost = 75
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = TIB_religious_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TIB_request_religious_freedom"
			set_country_flag = { flag = TIB_religious_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.9 days = 3 } }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 2
				check_variable = { CHI_TIB_autonomy_index < 35 }
			}
		}
	}

	TIB_request_development_aid = {
		allowed = { original_tag = TIB }
		icon = GFX_decision_generic_industry

		cost = 50
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = TIB_development_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TIB_request_development_aid"
			set_country_flag = { flag = TIB_development_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.10 days = 3 } }
		}

		ai_will_do = {
			base = 5
		}
	}

	TIB_organize_protest = {
		allowed = { original_tag = TIB }
		icon = GFX_decision_generic_protests

		days_remove = 180

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			check_variable = { CHI_TIB_autonomy_index < 40 }
			NOT = { has_idea = TIB_unrest }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TIB_organize_protest"
			add_to_variable = { CHI_TIB_autonomy_index = 15 }
			add_stability = -0.07
			add_ideas = TIB_unrest
		}

		ai_will_do = {
			base = 2
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	###############################################
	### ETK REQUESTS ###
	###############################################

	ETK_request_economic_development = {
		allowed = { original_tag = ETK }
		icon = GFX_decision_generic_industry

		cost = 50
		days_remove = 365

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = ETK_development_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ETK_request_economic_development"
			set_country_flag = { flag = ETK_development_request_pending days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.11 days = 3 } }
		}

		ai_will_do = {
			base = 5
		}
	}

	ETK_request_cultural_rights = {
		allowed = { original_tag = ETK }
		icon = GFX_decision_generic_political_discourse

		cost = 75
		days_remove = 730

		visible = {
			is_subject_of = CHI
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_war_with = CHI }
			NOT = { has_country_flag = ETK_cultural_request_pending }
		}

		cancel_trigger = {
			NOT = { is_subject_of = CHI }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ETK_request_cultural_rights"
			set_country_flag = { flag = ETK_cultural_request_pending days = 1095 }
			CHI = { country_event = { id = CHI_SAR_relations.12 days = 3 } }
		}

		ai_will_do = {
			base = 2
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	###############################################
	### TAIWAN — CHI SOFT POWER DECISIONS ###
	###############################################

	CHI_fund_TAI_probeijing_parties = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_chi_nko_friendship

		cost = 50
		days_remove = 180

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
		}

		available = {
			NOT = { has_war_with = TAI }
			TAI = { exists = yes }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_fund_TAI_probeijing_parties"
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { country_event = { id = CHI_SAR_relations.23 days = 30 } }
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_TAI_investment_incentives = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_industry

		cost = 100
		days_remove = 365

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
		}

		available = {
			NOT = { has_war_with = TAI }
			TAI = { exists = yes }
			NOT = { TAI = { has_idea = TAI_cross_strait_economic_ties } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_investment_incentives"
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_timed_idea = { idea = TAI_cross_strait_economic_ties days = 730 } }
		}

		ai_will_do = {
			base = 8
			modifier = {
				factor = 2
				check_variable = { treasury > 150 }
			}
		}
	}

	CHI_TAI_tourism_visa_program = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_political_discourse

		cost = 25
		days_remove = 365

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
		}

		available = {
			NOT = { has_war_with = TAI }
			TAI = { exists = yes }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_tourism_visa_program"
			set_temp_variable = { percent_change = 3 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_opinion_modifier = { target = CHI modifier = mutual_trade_agreement } }
		}

		ai_will_do = {
			base = 8
		}
	}

	CHI_TAI_academic_exchange = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_generic_political_discourse

		cost = 50
		days_remove = 365

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
		}

		available = {
			NOT = { has_war_with = TAI }
			TAI = { exists = yes }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_academic_exchange"
			set_temp_variable = { percent_change = 4 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_opinion_modifier = { target = CHI modifier = mutual_trade_agreement } }
		}

		ai_will_do = {
			base = 6
		}
	}

	###############################################
	### TAIWAN — CHI MILITARY PRESSURE DECISIONS ###
	###############################################

	CHI_PLAAF_ADIZ_incursion = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_peoples_republic_of_china

		cost = 30
		days_remove = 90

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
			NOT = { has_war_with = TAI }
		}

		available = {
			TAI = { exists = yes }
			has_war_support > 0.30
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_PLAAF_ADIZ_incursion"
			set_temp_variable = { percent_change = 3 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_stability = -0.02 }
		}

		ai_will_do = {
			base = 8
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_PLA_strait_exercise = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_peoples_republic_of_china

		cost = 50
		days_remove = 180

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
			NOT = { has_war_with = TAI }
		}

		available = {
			TAI = { exists = yes }
			has_war_support > 0.35
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_PLA_strait_exercise"
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_stability = -0.05 }
			add_war_support = 0.02
		}

		ai_will_do = {
			base = 6
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_TAI_missile_test = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_secret_rearmament

		cost = 75
		days_remove = 365

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
			NOT = { has_war_with = TAI }
		}

		available = {
			TAI = { exists = yes }
			has_war_support > 0.40
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_missile_test"
			set_temp_variable = { percent_change = 8 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_stability = -0.08 }
			add_war_support = 0.03
			country_event = { id = CHI_SAR_relations.22 days = 7 }
		}

		ai_will_do = {
			base = 4
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_TAI_economic_embargo_threat = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_trade

		cost = 50
		days_remove = 180

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
			NOT = { has_war_with = TAI }
		}

		available = {
			TAI = { exists = yes }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_economic_embargo_threat"
			set_temp_variable = { percent_change = 5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
			TAI = { add_stability = -0.03 }
		}

		ai_will_do = {
			base = 6
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	CHI_TAI_ultimatum_decision = {
		allowed = { original_tag = CHI }
		icon = GFX_decision_peoples_republic_of_china

		fire_only_once = yes
		cost = 150

		visible = {
			TAI = { exists = yes  NOT = { is_subject_of = CHI } }
			NOT = { has_war_with = TAI }
		}

		available = {
			TAI = { exists = yes }
			TAI = { influence_higher_70 = yes }
			has_completed_focus = CHI_TAI_strait_ultimatum
			has_war_support > 0.50
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHI_TAI_ultimatum_decision"
			TAI = { country_event = { id = CHI_SAR_relations.21 days = 3 } }
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	###############################################
	### TAIWAN — TAI DEFENSE DECISIONS ###
	###############################################

	TAI_strengthen_usa_ties = {
		allowed = { original_tag = TAI }
		icon = GFX_decision_diplomacy

		cost = 75
		days_remove = 365

		visible = {
			NOT = { is_subject_of = CHI }
			CHI = { exists = yes }
		}

		available = {
			USA = { exists = yes }
			NOT = { has_country_flag = TAI_usa_ties_pending }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TAI_strengthen_usa_ties"
			set_country_flag = { flag = TAI_usa_ties_pending days = 730 }
			add_opinion_modifier = { target = USA modifier = mutual_trade_agreement }
			USA = { add_opinion_modifier = { target = TAI modifier = mutual_trade_agreement } }
			set_temp_variable = { percent_change = -5 }
			set_temp_variable = { tag_index = CHI }
			set_temp_variable = { influence_target = TAI }
			change_influence_percentage = yes
		}

		ai_will_do = {
			base = 8
			modifier = {
				factor = 2
				CHI = { has_war_support > 0.4 }
			}
		}
	}

	TAI_arms_purchase = {
		allowed = { original_tag = TAI }
		icon = GFX_decision_generic_military_operation

		cost = 100
		days_remove = 365

		visible = {
			NOT = { is_subject_of = CHI }
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_country_flag = TAI_arms_purchase_pending }
			USA = { exists = yes }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TAI_arms_purchase"
			set_country_flag = { flag = TAI_arms_purchase_pending days = 365 }
			add_timed_idea = { idea = TAI_civil_defense_readiness days = 730 }
			CHI = { country_event = { id = CHI_SAR_relations.24 days = 7 } }
		}

		ai_will_do = {
			base = 6
			modifier = {
				factor = 2
				CHI = { has_war_support > 0.45 }
			}
		}
	}

	TAI_semiconductor_diplomacy = {
		allowed = { original_tag = TAI }
		icon = GFX_decision_buy_microchips_taiwan

		cost = 50
		days_remove = 365

		visible = {
			NOT = { is_subject_of = CHI }
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_country_flag = TAI_semiconductor_pending }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TAI_semiconductor_diplomacy"
			set_country_flag = { flag = TAI_semiconductor_pending days = 730 }
			add_timed_idea = { idea = TAI_diplomatic_recognition_push days = 730 }
			add_opinion_modifier = { target = USA modifier = mutual_trade_agreement }
			if = {
				limit = { JAP = { exists = yes } }
				add_opinion_modifier = { target = JAP modifier = mutual_trade_agreement }
			}
		}

		ai_will_do = {
			base = 7
		}
	}

	TAI_civil_defense_mobilization = {
		allowed = { original_tag = TAI }
		icon = GFX_decision_war_and_peace

		cost = 75
		days_remove = 365

		visible = {
			NOT = { is_subject_of = CHI }
			CHI = { exists = yes }
		}

		available = {
			NOT = { has_country_flag = TAI_civil_defense_pending }
			NOT = { has_idea = TAI_civil_defense_readiness }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TAI_civil_defense_mobilization"
			set_country_flag = { flag = TAI_civil_defense_pending days = 730 }
			add_war_support = 0.05
			add_timed_idea = { idea = TAI_civil_defense_readiness days = 730 }
		}

		ai_will_do = {
			base = 5
			modifier = {
				factor = 2
				CHI = { has_war_support > 0.40 }
			}
		}
	}
}
