investments_build_triggers = {
    if = {
        limit = { check_variable = { MD_investments_selected_building = 0 } }

        free_building_slots = {
            building = industrial_complex
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 1 } }

        free_building_slots = {
            building = arms_factory
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 2 } }

        free_building_slots = {
            building = dockyard
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 3 } }

        free_building_slots = {
            building = infrastructure
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 4 } }

        free_building_slots = {
            building = offices
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 5 } }

        free_building_slots = {
            building = anti_air_building
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 6 } }

        free_building_slots = {
            building = radar_station
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 7 } }

        free_building_slots = {
            building = air_base
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 8 } }

        free_building_slots = {
            building = fuel_silo
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 9 } }

        free_building_slots = {
            building = internet_station
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 10 } }

        free_building_slots = {
            building = renewable_energy_infra
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 11 } }

        free_building_slots = {
            building = fossil_powerplant
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 12 } }

        free_building_slots = {
            building = nuclear_reactor
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { MD_investments_selected_building = 13 } }

        free_building_slots = {
            building = agriculture_district
            size > amount
            include_locked = no
        }
    }
}

investments_build_trigger_by_index = {
    if = {
        limit = { check_variable = { index = 0 } }

    set_temp_variable = { amount = 5 }
        free_building_slots = {
            building = industrial_complex
            size > var:amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 1 } }

        free_building_slots = {
            building = arms_factory
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 2 } }

        free_building_slots = {
            building = dockyard
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 3 } }

        free_building_slots = {
            building = infrastructure
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 4 } }

        free_building_slots = {
            building = offices
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 5 } }

        free_building_slots = {
            building = anti_air_building
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 6 } }

        free_building_slots = {
            building = radar_station
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 7 } }

        free_building_slots = {
            building = air_base
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 8 } }

        free_building_slots = {
            building = fuel_silo
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 9 } }

        free_building_slots = {
            building = internet_station
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 10 } }

        free_building_slots = {
            building = renewable_energy_infra
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 11 } }

        free_building_slots = {
            building = fossil_powerplant
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 12 } }

        free_building_slots = {
            building = nuclear_reactor
            size > amount
            include_locked = no
        }
    }
    else_if = {
        limit = { check_variable = { index = 13 } }

        free_building_slots = {
            building = agriculture_district
            size > amount
            include_locked = no
        }
    }
}